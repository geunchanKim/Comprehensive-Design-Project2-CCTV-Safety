# Git Convention — 지능형 CCTV 기반 산업현장 안전관제 시스템 (종프2)

> 팀 전체가 지켜야 하는 브랜치·커밋·PR 규칙 정리본.

---

## 1. 브랜치 전략

| 브랜치 | 역할 |
|---|---|
| `main` | 항상 배포/발표 가능한 상태 유지 |
| `develop` | 팀 작업 통합 브랜치 |
| `feat/기능이름` | 새 기능 개발 (예: `feat/object-detection`) |
| `fix/버그이름` | 버그 수정 |
| `docs/문서이름` | 문서 작업 (README, 회의록 등, 예: `docs/readme`) |
| `chore/작업이름` | 설정, 컨벤션 등 잡무 (예: `chore/git-convention`) |
| `refactor/대상이름` | 동작 변경 없는 코드 개선 |

- 브랜치명은 영문 소문자 + 하이픈(`-`) 사용
- 브랜치 접두어는 커밋 메시지 접두어와 동일한 체계 사용 (하나만 외우면 됨)
- `main`, `develop`에 직접 push 금지 — 반드시 PR로만 반영
- 작업 끝난 브랜치는 머지 후 삭제

## 2. 병합(merge) 타이밍 규칙

- 모든 종류의 브랜치(`feat/`, `fix/`, `docs/`, `chore/`, `refactor/`)에 동일하게 적용
- 각 브랜치에서는 자유롭게 개발
- `develop`으로 합치는 건 원칙적으로 **일요일 병합 → 다음 주 수요일 PR**로 진행
- **단, 이번 주 안에 리뷰받고 싶은 작업은 수요일 18:00 전까지 `develop`에 merge**
- merge는 리뷰를 최소 한 명 이상 받은 뒤 진행 (바로 merge 버튼 누르지 않기)
- `develop` → `main`은 중간발표/최종발표 등 제출 시점에 맞춰 별도 협의 후 진행

## 3. 커밋 메시지 컨벤션

**소문자로 작성**하며, 아래 접두어 중 하나로 시작.

| 접두어 | 용도 |
|---|---|
| `feat:` | 새로운 기능 추가 (탐지 모델, API, 대시보드 화면 등) |
| `fix:` | 버그 수정 |
| `docs:` | 문서 수정 (제안서, README, 회의록 등) |
| `style:` | 코드 포맷팅, 세미콜론 누락 등 코드 변경이 없는 경우 |
| `refactor:` | 코드 리팩토링 (구조 개선, 성능 개선) |
| `test:` | 테스트 코드 추가 |
| `chore:` | 빌드 업무, 패키지 관리, CI/CD, 환경 세팅 등 |
| `design:` | CSS 등 UI/대시보드 디자인 변경 |
| `rename:` | 파일명(또는 폴더명) 수정 |
| `remove:` | 코드(파일) 삭제 |

**예시**
```
chore: cctv 영상처리 서비스 docker 설정 추가
fix: 위험구역 경계 좌표 계산 오류 수정
feat: yolo 기반 객체탐지 모듈 초기 로직 추가
```

**커밋 메시지 템플릿 적용 (필수)**

레포 루트의 `.gitmessage.txt`를 각자 로컬에 아래 명령어로 한 번 등록하면, `git commit` 실행 시 접두어 안내가 자동으로 뜹니다.

```
git config commit.template .gitmessage.txt
```

**판단 기준 참고**
- 연결 확인용 임시 엔드포인트, 환경 설정, 패키지 추가 → `chore`
- 실제로 서비스에 쓰이는 기능(탐지 로직, API, 대시보드) → `feat`
- 오류·버그 수정 → `fix`

## 4. PR 이름 규칙

```
접두어: 작업 내용 
```

**예시**
```
chore: ai 모듈 초기 세팅 
fix: 위험구역 좌표 오류 수정 
```

- 접두어는 커밋 컨벤션과 동일한 규칙 사용
- PR 본문 형식은 `.github/PULL_REQUEST_TEMPLATE.md`에 별도로 정의되어 있으며, PR 생성 시 자동으로 채워짐

## 5. 리뷰 규칙

- PR 올리기 전 셀프 리뷰(diff 확인) 한 번 하기
- 리뷰어는 24시간 이내 확인 원칙 (급한 건은 팀 채널에 별도 공지)
- 셀프 머지 금지
- merge는 리뷰를 최소 한 명 이상 받은 뒤 진행

## 6. 폴더/네이밍 주의사항

### 최상위 폴더 구조

```
├── ai/         # YOLO 탐지·추적 모델, 위험 판정 로직
├── backend/    # FastAPI 백엔드 (API, 실시간 알림)
├── frontend/   # React 대시보드
├── infra/      # 배포·인프라 설정 (Docker, compose.yaml, 서버 설정 등)
├── docs/       # 회의록, 계획서 등 문서
├── CONVENTION.md
└── .github/
```

### 파일 네이밍 규칙

| 영역 | 규칙 |
|---|---|
| `ai/`, `backend/` (Python) | snake_case (예: `object_detector.py`) |
| `frontend/` (React/JS) | 컴포넌트 파일은 PascalCase (예: `Dashboard.jsx`), 나머지는 camelCase |
| `infra/` | 소문자 + 하이픈 (예: `docker-compose.yaml`) |

### 폴더명 표기 주의

- 대소문자를 정확히 지킬 것 — 서버 배포 환경(Linux)은 대소문자를 구분하므로 로컬(Mac/Windows)에서 문제없어도 배포 시 깨질 수 있음
- 폴더명 변경 시 팀 채널에 공지 후 진행 (다른 팀원 로컬 경로 꼬임 방지)

*본 문서는 팀 협의에 따라 지속적으로 업데이트됩니다.*