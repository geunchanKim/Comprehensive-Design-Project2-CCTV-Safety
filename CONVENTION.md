# Git Convention

지능형 CCTV 기반 산업현장 안전관제 시스템 (종합설계프로젝트2) 팀의 Git 사용 규칙입니다.

## 브랜치 전략

| 브랜치 | 용도 |
|---|---|
| `main` | 항상 동작하는 안정 버전만 유지 (발표/제출용) |
| `feat/기능명` | 새로운 기능 개발 |
| `fix/버그명` | 버그 수정 |
| `docs/문서명` | 문서 작업 (README, 회의록 등) |
| `chore/작업명` | 설정, 컨벤션 등 잡무성 작업 |
| `refactor/대상명` | 동작 변경 없는 코드 개선 |

**규칙**
- `main`에 직접 push 금지, 반드시 PR을 통해 머지
- 브랜치명은 영문 소문자 + 하이픈(`-`) 사용 (예: `feat/object-detection`)
- 작업이 끝난 브랜치는 머지 후 삭제

## 커밋 메시지 규칙

```
<type>: <제목>

<본문 (선택)>
```

**type 종류**

| type | 설명 |
|---|---|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 |
| `style` | 코드 포맷팅, 세미콜론 등 (로직 변경 없음) |
| `refactor` | 코드 리팩토링 (기능 변경 없음) |
| `test` | 테스트 코드 추가/수정 |
| `chore` | 빌드, 설정, 기타 잡무 |

**작성 규칙**
- 제목은 50자 이내, 끝에 마침표 없이
- 제목은 명령형으로 작성 ("추가함" X → "추가" O)
- 본문이 필요하면 제목과 한 줄 띄우고 작성 (무엇을, 왜 했는지 위주)

**예시**
```
feat: YOLO 기반 객체탐지 모듈 초기 구현
fix: 위험구역 경계 좌표 계산 오류 수정
docs: README에 프로젝트 개요 및 설치 방법 작성
chore: 깃 컨벤션 문서 초안 작성
```

## PR(Pull Request) 규칙

- PR 제목은 커밋 메시지 컨벤션과 동일하게 작성
- 본인 작업은 팀원 1명 이상의 리뷰를 받은 후 머지
- 셀프 머지 금지
- 머지 방식: **Squash and merge** 사용 (커밋 히스토리 정리)
- PR 설명에 다음 포함:
  - 작업 내용 요약
  - 관련 이슈 번호 (있다면 `Closes #이슈번호`)
  - 테스트/확인 방법 (필요 시)

## 이슈 관리

- 작업 시작 전 이슈로 등록 후 진행 권장
- 라벨 예시: `bug`, `enhancement`, `docs`, `question`
- 이슈와 PR 연결 시 `Closes #12` 형식 사용

## 코드 스타일 (추후 논의)

- Python: PEP8 준수, 가능하면 Black 포매터 사용
- 변수/함수명: `snake_case`
- 폴더 구조: `src/`, `models/`, `data/`, `docs/` 등으로 역할별 분리

---

*본 문서는 팀 협의에 따라 지속적으로 업데이트됩니다.*